# Where the host looks for the MIS micro frontend's remoteEntry.js.
# Dev default is the analytics-portal-mis dev server; point this at your CDN /
# static host in CI so a rebuilt remote is picked up without rebuilding the host.
MIS_REMOTE_URL=http://localhost:3001/remoteEntry.js
